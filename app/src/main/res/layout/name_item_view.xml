<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/game_name_item_background"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/name_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_weight="4"
        android:fontFamily="sans-serif-medium"
        android:padding="@dimen/game_name_item_padding"
        android:textColor="@color/game_name_item_text"
        android:textSize="@dimen/game_name_item_text_size"
        tools:text="Test Name" />

    <TextView
        android:id="@+id/name_count_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_weight="1"
        android:fontFamily="sans-serif-medium"
        android:gravity="end"
        android:padding="@dimen/game_name_item_padding"
        android:textColor="@color/game_name_item_text"
        android:textSize="@dimen/game_name_item_text_size"
        tools:text="(2)" />

</LinearLayout>